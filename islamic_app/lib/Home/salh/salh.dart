import 'package:flutter/material.dart';

class SalhTab extends StatelessWidget {
  const SalhTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.yellow);
  }
}
